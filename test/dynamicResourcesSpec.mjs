import jasmine from "./jasmine";
import { execute, setResource } from "../lib/index.mjs";

const { describe, it, pending } = jasmine.env;

describe("dynamic resources", () => {
	describe("execute", () => {
		it("must accept dynamic resources.", () => {
			pending("unfinished test");
		});
		it("must supply a single dynamic resource to a plan", () => {
			pending("unfinished test");
		});
		it("must supply multiple dynamic resources to a plan", () => {
			pending("unfinished test");
		});
		it("must supply a dynamic resource to multiple plans", () => {
			pending("unfinished test");
		});
		it("must supply a dynamic resource after a static resource", () => {
			pending("unfinished test");
		});
	})
});
