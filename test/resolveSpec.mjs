import jasmine from "./jasmine";
import { resolve } from "../lib/index.mjs";

const { describe, it, pending } = jasmine.env;

describe("resolve", () => {
	it("does not mutate valid plans", () => {
		pending("unfinished test");
	});
	it("imports external plans", () => {
		pending("unfinished test");
	});
	it("throws for invalid plans", () => {
		pending("unfinished test");
	});
	it("throws for invalid actions", () => {
		pending("unfinished test");
	});
});
